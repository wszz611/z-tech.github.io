问题:

在开发自制 STM32F103 板子时，遇到一个诡异现象：Keil MDK 通过 ST-Link 可以正常烧录调试，但换到 VS Code（使用 ST-Link GDB server）却反复报错。


原因分析：

官方 GDB Server 容错低：ST 官方调试服务器非常“死板”，遇到芯片处于读保护（RDP）、低功耗状态或自焊板子连线不稳定时，会直接放弃连接。

VS Code 初始化协议敏感：引脚锁死、读保护状态、频率过高都会导致它在握手时失败。

解决方法：

使用vscode里面的openocd进行烧录以及调试即可